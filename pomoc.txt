#vzpostavitev dockerja
  docker-compose up

#povezava AtlasDB v docker
  docker exec -it sp-etv-mongodb bash -c "mongo \"mongodb+srv://etv.pact2.mongodb.net/etv\" --username app"

#nastavitev delovanja heroku za produkcijsko bazo
  heroku config:set MONGODB_CLOUD_URI='mongodb+srv://app:SPetv2020@etv.pact2.mongodb.net/etv?retryWrites=true&w=majority'

#prenos iz lokalne v produkcijsko bazo
  docker exec -it sp-etv-mongodb bash -c "mongodump -h localhost -d etv -o ./"
  docker exec -it sp-etv-mongodb bash -c "mongorestore --uri mongodb+srv://app:SPetv2020@etv.pact2.mongodb.net --ssl --username app --authenticationDatabase admin -d etv ./etv"

#izbira heroku baze
  heroku config:set NODE_ENV='production' -a debtsettler
  heroku config:set JWT_GESLO=n3k4jv4rn3g4
  heroku config:set JWT_GESLO=n3k4jv4rn3g4 -a debtsettler

#povezovanje na produkcijsko bazo
  NODE_ENV='production' MONGODB_CLOUD_URI='mongodb+srv://app:SPetv2020@etv.pact2.mongodb.net/etv?retryWrites=true&w=majority' nodemon

#mongo ukazi za uporabo baze
  docker exec -it sp-etv-mongodb mongo
  use etv
  exit

#rocni vnos uporabnika v mongo
  docker exec -it sp-etv-mongodb mongo

  db.uporabniki.save({
    ime : 'Mark',
    priimek : 'Breznik',
    vzdevek : 'mbdev',
    email : 'mark.breznik@gmail.com',
    telstevilka : '040490109',
    geslo : 'SP123'
    })

    db.nakupi.save({
      imeTrgovine : 'Meractor',
      opisNakupa : 'Kruh',
      cenaNakupa : '99',
      avtor : '5fc2bebd885f8c33cc35d534'
      })

      db.nakupovalniSeznam.save({
        naslov : 'kruh',
        opis : 'polbel prosm',
        })

#rocni izpise vse uporabnike
  db.uporabniki.find().pretty()


# test api
curl -X POST \
       -d "ime=Miha&priimek=Lavbic&vzdevek=mihec&email=dejan@lavbic.net&telstevilka=040444222&geslo=Test1234" \
       -H "Content-Type: application/x-www-form-urlencoded" \
       http://localhost:3000/api/registracija

curl -X POST \
      -d "email=blaz.pridgar@gmail.com&geslo=Nekajvarnega4" \
      -H "Content-Type: application/x-www-form-urlencoded" \
      http://localhost:3000/api/prijava
